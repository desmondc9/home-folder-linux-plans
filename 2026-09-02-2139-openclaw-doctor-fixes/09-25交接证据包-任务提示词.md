# 晨报周报交接·09-25 证据包（一次性任务）

今天 2026-09-25 周五，时区 Asia/Shanghai。背景：晨报/周报定时任务交接验收，KimiClaw 做影子跑，需要正式版运行证据。你要收集证据并发到 Kimi 群。全程只读与收集：禁止修改任何报告文件，禁止改动任何 job 配置。

## 步骤

1. `export PATH="$HOME/.nvm/versions/node/v26.8.1/bin:$PATH"`。
2. 五个 job 逐一 `openclaw automations get <id>`，提取 lastRunAtMs（换算北京时间：`date -d @$((lastRunAtMs/1000)) '+%F %T %Z'`）、lastRunStatus、lastDurationMs、lastDelivered、lastDeliveryStatus；若状态显示仍在运行（有 runningAtMs / lastRunStatus 非 ok），如实记「运行中」，不要等待。
   - 晨报生成 c93a7761-7fd8-40b5-ac86-f451712424af
   - 晨报转发 7b1a652a-eec6-47d9-a94e-aea162472815
   - 周报生成 d1643cb0-2ba7-499e-b1e2-8f6adda8be8a
   - 周报转发 d1d64a65-0e28-4c8c-8afe-eb66cbb2d9ca
   - 上游扫描 72bbfb86-6a82-4563-a4e1-911abc3e75c8（今晨 06:00 已跑，确认状态/时长/哨兵文件时间戳即可；其 delivered=false/not-requested 亦属设计内）
3. 对同一批 id 执行 `openclaw cron runs --id <id> | head -30`，取今日 run 的开始时间、时长、delivered。session_key 统一注明：`agent:main:cron:<jobId>`（transcript 全文取法，交接清单 §4 有记载）。
4. 看门狗情况：`openclaw automations get c43b6520-890b-44ba-afb0-8d0f894c0100`（报告流水线看门狗），看状态与最近活动；再用 `ls -la --time-style=full-iso /home/desmond/reports/全球机构研究报告库/日报/2026-09-25*` 记录各文件修改时间（晚于 07:20 即提示可能有补跑链路，如实记录）。
5. 周报产物：先 `date +%G年第%V周` 确认本周周号（预期 2026年第39周），`ls -la /home/desmond/reports/全球机构研究报告库/周报/`；`wc -l /home/desmond/reports/全球机构研究报告库/INDEX.md`。
6. 汇总成紧凑中文证据包：每个 job 一行（名称｜id｜session_key｜开始时间｜时长｜状态｜delivered），加产物路径与字节数、看门狗与异常时间点。必须注明：**扫描与两个生成 job 的 delivery=none、delivered=false 属设计内正常，送达以两个转发 job delivered=true 为准**。用 write 保存到 `/home/desmond/.openclaw/workspace/kimi-group-chat/ClawMeeting/09-25证据包.md`。
7. 用 exec 发群：`kimiim-cli send-message 1a0d0c70-6792-8369-8000-0cf5d9b2bb12 "晨报周报 09-25 正式版证据包：四个 job 的 session_key、开始时间、时长、状态、delivered，产物落盘路径，看门狗与异常时间点，见附件。" --file /home/desmond/.openclaw/workspace/kimi-group-chat/ClawMeeting/09-25证据包.md`。失败则重试一次；再失败不重试。
8. 最终回复=一句话结果：成功写「证据包已发群」；失败写「kimiim 两次发送失败，证据文件已落盘 <路径>」。
