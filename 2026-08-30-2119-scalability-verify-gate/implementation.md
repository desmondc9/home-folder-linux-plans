# 实施记录

**日期**: 2026-08-30
**对应设计**: [spec.md](./spec.md)

## 1. 取材

先把某企业级项目仓库的记忆与档案读了一遍(只读,未改动该仓库任何文件):

| 来源 | 提炼出的内容 |
|---|---|
| 生产慢查询调查报告(24h 窗口) | DB CPU 空闲但单条查询 100–186s;后台 handler 与用户请求同实例;跨洋 RTT;`containerConcurrency=1000` 偏高 |
| 同一调查的 30 天窗口报告 | 24h 窗口错过故障期;单桶 30 天分位数失真;change-stream 长轮询霸榜需剔除;发版时间线二分法;retry storm 与 15.00s deadline |
| 前端"卡在登录页"事件报告 | SPA 陈旧 chunk;404 只要 0.07s 所以延迟监控全绿;日志型 4xx 指标 + 前端 RUM 是唯一信号;nginx 缓存头三件套 |
| 数据访问层审计(545 个 mapper / ~3400 条 SQL) | 反模式全量计数与治理优先级;零索引 vs 过度索引;非 SARGable 全家桶;拼串破坏计划缓存;乐观锁双路径缺口;陈旧读 0 处 |
| 虚拟线程事故复盘 | 方法体内 new executor;`synchronized` pinning;carrier 池进程级全局 + CPU 配额小 ⇒ 全服务冻结零日志 |
| 缓存设计文档 | key 粒度由失效成本决定;Redis 故障非致命 |
| WAF 403 修复设计 | 双重编码 payload 命中 SQLi 规则;后端零日志;改契约而不是放宽规则;部署顺序 |
| 项目规范文件 | multipart 上传路径白名单约定;异常处理器吞日志;批量参数上限分块;租户隔离既是安全也是性能;字段名漂移 |
| 架构文档的 infra 实测快照章节 | §0.2 infra 盘点清单的原型(算力/DB/缓存/MQ/网关/存储/调度/网络/环境阶梯) |
| backlog 档案 | CI `test.failure.ignore` 放过真实回归;Liquibase 迁移与启动探针窗口 |

## 2. 改动

### 2.1 `~/docs/scalability-review-checklist.md`(139 行 → 382 行)

- 文件头补「三个使用时机」,与 `~/CLAUDE.md` 的 Step A/B、Step C、Step E 对应。
- **§0 重构为三段**:
  - §0.1「数据从哪来:先查可观测性平台,查不到再问人」——六档决策顺序表;GCP / Azure / Grafana 栈 / 数据库原生统计 四张取数速查表;"都拿不到时问用户的 5 个具体问题";**七条取数方法学的坑**。
  - §0.2「摸清 infra 家底(容量预估的分母)」——十项盘点清单表 + 只读遍历做法 + 硬限制专表(SQL 参数上限 / 请求超时 / 体积上限 / MQ / 缓存 / WAF / 平台配额)。
  - §0.3 原有的容量数字,补"理论上限还剩几倍余量""多租户看单租户最大值""用户侧还是服务端口径"。
- §1–§11 逐节补入 §3 表格所列教训(见 spec.md);§3 结尾加"全量机械扫描 + 与生产慢查询对账"的审计方法论;§8 更名为「边缘与传输层(LB / WAF / 网关 / CDN)」。
- **新增 §12「Verify 阶段性能门禁」**:§12.1 触发器 9 条、§12.2 收口检查 16 问(五组)、§12.3 产出物规格。
- 速查表 6 行 → 14 行;"如何使用这份清单"补入门禁与取数优先级。

### 2.2 `~/CLAUDE.md`(改动后 347 行)

- 循环示意图补 D+ 与新的 E,并顺延 F/G。
- Step A 新增产出物「A capacity & infrastructure baseline」(两部分:优先自取的基线数字 + infra 盘点);收尾 checklist 同步。
- Step B 新增"性能决策必须落进计划"段落 + checklist 同步。
- Step C 新增第 7 条编码红线。
- **新增 Step E「Performance & scalability verify gate (mandatory)」**:4 步 + 收尾 checklist;并指明"整体架构评审走 §0–§11,不是这个门禁"。
- 原 Step E → Step F(文档表两行补性能相关触发条件),原 Step F → Step G(提交前确认项 + PR 描述项各加一条)。
- 全文 4 处 Step 交叉引用同步(94、130 行的 "Step F below" → "Step G below")。
- 「Reference checklists」条目从一句话扩成三种用法 + 事故来源说明。

## 3. 验证

```bash
# Step 字母序列自洽(无残留的旧 Step E/F 指向)
grep -n "Step [0-9A-G]\|Step D+" ~/CLAUDE.md
```

- 结果:`Step 0 / A / B / C / D / D+ / E / F / G` 齐全且唯一;两处 "Step G below" 已指向新的提 PR 步骤;`Step D+` 的两处容器栈清理引用保持不变(正确)。
- 两份文档均通读复查,无内容自相矛盾;`~/docs` 不在任何 git 仓库内,故无需提交,本档案即变更记录。
- 提交前 `gitleaks dir .` 扫描本仓库工作区,确认无 leaks;成品中不含任何客户/项目/域名/工单标识(设计与实施文档统一以"某企业级 GCP + 分布式 SQL 数据库项目"指代来源)。

## 4. 遗留

- 清单里的取数命令以 GCP 为主(取材来源如此),Azure / Grafana 两栏只到"从哪拿"的粒度;下次真在 Azure 项目上跑一遍时,把实际用的 KQL 补进 §0.1。
- §12 门禁尚未在真实任务上跑过一轮;首次使用后按实际体感回来调触发器与问题数量(过重会被绕开,是这类门禁的主要失败模式)。
